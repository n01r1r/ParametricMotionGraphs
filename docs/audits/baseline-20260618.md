# Baseline Audit - 2026-06-18

## Scope

Freeze current branch state before Session 0.2+ work.

- No code changes in this audit.
- Baseline taken from existing `build/` tree and current worktree.

## Repository State

- Branch: `codex-viewer-runtime-lifecycle`
- Commit: `351d4ae90acd6d5f9dffdf89295cd9eb6931b58e`

## Worktree State

`git status --short --branch`

```text
## codex-viewer-runtime-lifecycle...origin/codex-viewer-runtime-lifecycle
 M CMakeLists.txt
 M apps/viewer/PmgViewerWorkspace.cpp
 M apps/viewer/PmgViewerWorkspace.h
 M apps/viewer/PmgViewerWorkspaceGraph.cpp
 M tests/test_viewer_graph_runtime.cpp
?? .codegraph/
?? PMGFullPaper.tex
?? apps/viewer/ViewerRuntimeModule.cpp
?? apps/viewer/ViewerRuntimeModule.h
?? docs/
```

## Build Baseline

Command:

```powershell
cmake --build build --config Debug
```

Result:

- Overall result: failed
- Failure class: environment failure
- Failure point: `pmg_cli.vcxproj`
- Successful targets before failure:
  - `glfw3.lib`
  - `glm.lib`
  - `imgui_backend.lib`
  - `glewd.lib`
  - `pmg_core.lib`
  - `viewer_host.lib`
  - `pmg_viewer_workspace.lib`
  - `pmg_viewer.exe`

Observed MSBuild error:

```text
error MSB6001
System.ArgumentException
existing key: 'Path'
added key: 'PATH'
```

Interpretation:

- This is not evidence of runtime or test regression in repo code.
- This is Windows environment/toolchain state: duplicate `Path`/`PATH` keys reaching MSBuild / `ProcessStartInfo`.
- Separate PowerShell check also failed while enumerating `Env:` with same duplicate-key exception.

## CTest Baseline

Command:

```powershell
ctest --test-dir build -C Debug --output-on-failure
```

Result:

- 35 / 35 tests passed
- 0 failed
- Total wall time: `7.89 sec`

Covered binaries/tests observed in pass set:

- Core/unit: `test_pose_blend`, `test_runtime_controller`, `test_transition_quality`, `test_root_canonicalization`, others
- CLI/integration: `cli_validate_demo_walk_2d`, `cli_build_artifact_walk_2d`, `cli_goto_walk_2d`, `cli_build_artifact_selective`, `cli_build_artifact_box_shrink`
- Viewer/runtime: `test_viewer_host`, `test_viewer_graph_authoring`, `test_viewer_graph_runtime`

## Failure Classification

### Known Failures

- None observed in this baseline run.

### Environment Failures

- `cmake --build build --config Debug`
  - MSBuild `Path` vs `PATH` collision
  - Blocks clean full rebuild of targets that still need compilation
  - Most likely host environment / shell launch issue, not repo logic issue

### Real Regressions

- None proven by this baseline.
- Current test suite passed completely.

## Important Baseline Caveat

`cmake -LA -N build` reports:

```text
PMG_BUILD_TESTS:BOOL=OFF
PMG_BUILD_VIEWER:BOOL=ON
```

But current `build/` tree still contains registered tests and test executables from earlier configuration, and `ctest` still runs them successfully.

Implication:

- This baseline records current usable state of existing build tree.
- It does not prove a clean-from-scratch configure+build+test path on this machine until `Path`/`PATH` issue is fixed.

## Windows PATH / MSBuild Note

Symptoms:

1. `cmake --build build --config Debug` fails in MSBuild with duplicate key error.
2. `Get-ChildItem Env:` also fails with duplicate key error in this shell session.

What to check next:

1. Process environment contains both `Path` and `PATH`.
2. Shell startup scripts or launcher may inject duplicate case-variant keys.
3. Re-run build from clean Visual Studio Developer PowerShell / Developer Command Prompt after normalizing env.

Suggested verification commands after env fix:

```powershell
cmake --build build --config Debug
ctest --test-dir build -C Debug --output-on-failure
```

## Viewer Smoke Checklist

Target binary:

```text
build/Debug/pmg_viewer.exe
```

Launch options:

```powershell
build\Debug\pmg_viewer.exe
build\Debug\pmg_viewer.exe specs\demo_walk_2d_triangulated.pmg_spec
```

Manual checklist:

1. App starts, opens GLFW window, no immediate stderr abort.
2. `PMG Viewer` panel visible. `Viewer` panel visible.
3. `Inputs` tab lists BVH corpus files.
4. Load one clip -> skeleton appears, transport controls respond.
5. `Clip playback` mode: `Play/Pause`, `Reset`, `< Frame`, `Frame >`, `Phase` slider work.
6. `Motion Space` tab: add motion samples, parameter sliders move preview without crash.
7. `PMG Runtime -> From spec`: build/open canonical `demo_walk_2d_triangulated.pmg_spec`.
8. `Runtime` tab shows graph canvas, active node, requested parameter, transition section.
9. Double-click graph node or use `Set runtime target` -> request target changes.
10. For 2D node, clicking parameter canvas moves requested marker immediately.
11. During transition, `RUNTIME ACTUAL` becomes concrete value, not `pending`.
12. `completed transitions` increments after transition completion.
13. `Clear trace` clears trail/markers and leaves current root point as new seed.
14. `Restart graph` restarts runtime from current node/current parameter and resets trace.
15. Right-click floor sets goto target when graph runtime active.
16. `Clear target` clears goto state.
17. Camera controls work:
    - left drag orbit
    - scroll zoom
    - WASD fly
    - `Reset camera`
18. No obvious pose pop, root teleport, or UI-only crash during 30-60s runtime playback.

## Baseline Summary

- Automated tests: green
- Full rebuild: blocked by Windows environment issue
- Known failures: none
- Real regressions: none demonstrated
