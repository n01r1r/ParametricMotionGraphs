# Legacy three-anchor walk fixture retained for regression and audit commands.
# This is not a canonical demo: the measured signed turn response is
# non-monotone because the middle anchor reverses turn direction.
# Parameter 0..1 is therefore an authored path through three compatible walk
# clips, not a globally ordered physical "turn tightness" axis.
node walk 1
registration walk LeftAnkle LeftAnkle,RightAnkle 3 1
parameter_metric walk turn_rate
example walk 0.0 ../BVH/walkCurve.bvh
example walk 0.5 ../BVH/walkMoreCurve.bvh
example walk 1.0 ../BVH/walkTightCurve.bvh
expect walk spanned_axes 1
expect walk corner_coverage full
edge walk walk
# Thresholds calibrated for D6's asymmetric windows and raw weighted squared
# sum. Values are specific to this 31-joint corpus and five-frame window.
edge_phase_range walk walk 0.70 0.95 0.05 0.30
edge_config walk walk 225 250 12 60 7
