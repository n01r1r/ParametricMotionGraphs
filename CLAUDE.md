# Claude Code loader

Agent instructions live in [`AGENTS.md`](AGENTS.md) and are agent-agnostic
(Codex reads it too). This file only imports them so Claude Code loads the same
notes — keep the substance in `AGENTS.md`, not here.

@AGENTS.md
